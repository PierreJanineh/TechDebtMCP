# Example environment overrides for Tech Debt MCP
# Copy this to .env or set variables in your environment.

# Pin the server version explicitly. If set, the server will use this
# value instead of trying to read package.json.
TECH_DEBT_MCP_VERSION=2.0.0
